# Feishu gateway credentials live in the installed profile .env.
FEISHU_APP_ID=cli_xxx
FEISHU_APP_SECRET=secret_xxx
FEISHU_DOMAIN=feishu
FEISHU_CONNECTION_MODE=websocket
FEISHU_ALLOWED_USERS=ou_sre_1,ou_service_owner_1
FEISHU_REQUIRE_MENTION=true

# Read-only observability endpoints.
OBSERVE_PROMETHEUS_BASE_URL=https://prometheus.example.internal
OBSERVE_LOKI_BASE_URL=https://loki.example.internal

# Read-only Kubernetes access.
KUBECONFIG_READONLY=/secure/path/prod-aliyun-sg-intlsms-readonly.kubeconfig
KUBECTL_BIN=kubectl

# Optional secret broker / vault references.
DEVOPS_POLICY_ENDPOINT=https://policy.example.internal
DEVOPS_AUDIT_ENDPOINT=https://audit.example.internal

[project]
name = "loki-mcp"
version = "1.0.0"
description = "Read-only Loki MCP server for DevOps agents"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.12.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

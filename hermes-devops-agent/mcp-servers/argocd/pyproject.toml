[project]
name = "argocd-mcp"
version = "1.0.0"
description = "Read-only ArgoCD MCP server for DevOps agents"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.12.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

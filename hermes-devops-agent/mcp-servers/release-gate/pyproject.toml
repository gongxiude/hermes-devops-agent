[project]
name = "release-gate-mcp"
version = "1.0.0"
description = "Release approval decision MCP server for Software Delivery gated profiles"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.12.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

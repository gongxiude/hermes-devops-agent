[project]
name = "release-executor-mcp"
version = "1.0.0"
description = "Gated release execution MCP server for Software Delivery profiles"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.12.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

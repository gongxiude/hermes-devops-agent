[project]
name = "git-workspace-mcp"
version = "1.0.0"
description = "Controlled Git workspace MCP server for Software Delivery draft workflows"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.12.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

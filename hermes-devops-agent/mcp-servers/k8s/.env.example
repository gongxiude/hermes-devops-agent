# MCP_SERVER_NAME=k8s
# MCP_LOG_LEVEL=INFO

# kubectl binary path
KUBECTL_BIN=kubectl

# kubeconfig file (leave empty for default ~/.kube/config)
KUBECONFIG=

# Set to "false" to enable write tools (scale, patch, apply, delete, exec, etc.)
# Default is "true" (read-only mode)
K8S_READ_ONLY=true
